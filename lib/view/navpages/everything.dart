

import 'package:flutter/material.dart';

class Everything extends StatelessWidget {
  const Everything({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is everything page"),),
    );
  }
}
